{
  "name": "dream-llm",
  "private": true,
  "workspaces": [
    "clients/*",
    "services/*",
    "packages/*"
  ],
  "packageManager": "bun@1.3.14",
  "scripts": {
    "clean:ports": "for port in 3000 4000 5173; do lsof -ti :$port | xargs kill -9 2>/dev/null; done; echo 'Ports cleaned'",
    "dev": "bun run clean:ports && turbo run dev --filter=@autix/chat-web --filter=@autix/chat",
    "dev:web": "bun run clean:ports && turbo run dev --filter=@autix/web --filter=@autix/api",
    "dev:chat-web": "bun run clean:ports && turbo run dev --filter=@autix/chat-web",
    "dev:chat": "turbo run dev --filter=@autix/chat",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.8.20",
    "wait-on": "^9.0.5"
  }
}